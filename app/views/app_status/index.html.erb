<h1>Application Status</h1>
<% if user_signed_in? %>
<% if current_user.admin == true %>
  <h3>You are admin.</h3>
<% elsif current_user.user_type == true %>
  <h3>You are empl.</h3>
<% else %>
  <% if current_user.apply != nil %>
  <h3>Status:<%= current_user.apply.status %></h3>
  <% if current_user.apply.feedback != nil %>
    <h3>Feedback:<%= current_user.apply.feedback %></h3>
  <% end %>
  <% else %>
  <h2>You have not applied for pension. You can do so by clicking on <%= link_to "this link", new_apply_path %>.
  <% end %>
  <div class="well">
    <b>
      For any inquiries please <%= link_to "Contact", contact_index_path %>.
    </b>
  </div>
<% end %>
<% end %>
